# Node / editor
node_modules/
.DS_Store
Thumbs.db
*.log

# Chrome build / packaging artifacts
*.crx
*.pem
*.zip
src/_metadata/

# Internal docs / store assets / publishing notes (kept local, not published in repo)
docs/

# Local example exports (not code) — may contain real internal hostnames
json/
watermark-configs-*.json

# Scratch dir used for verification/patch scripts
tmp/

# Secrets (none today; guard for the phase-4 Supabase work)
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
